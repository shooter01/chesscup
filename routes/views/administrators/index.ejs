
<% layout('boilerplate') -%>


<div id="city_modal"></div>

<script>
    var teachers = JSON.parse('<%- teachers %>');
    var offices = JSON.parse('<%- offices %>');
</script>

<!-- Modal -->
<!--<div class="modal" id="addModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Добавление</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">


            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Закрыть</button>
                <button type="button" class="btn btn-primary" id="save_theme">Сохранить</button>
            </div>
        </div>
    </div>
</div>-->

<div id="users"></div>

<script src="/js/users.min.js"></script>

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">

<script>
    /*$(function () {

        var task_id = 0;

        $(".edit").on("click", function () {
            var id = $(this).data("id");
            task_id = id;
            $("#theme_title_edit").val(admins[id].title);

        });

        $("#save_theme").on("click", function () {
            var self = this;
            $(this).attr("disabled", "disabled");
            var title = $("#theme_title").val();
            if (title) {
                $.post("/city/create", {title:title},function (data) {
                    console.log(data);
                    if (data.status === "ok" && data.city_id) {
                        location.reload();
                    } else {
                        alert("Ошибка при создании темы");
                    }
                }, "json").always(function () {
                    $(self).removeAttr("disabled");
                });
            } else {
                alert("Title не указан");
                $(self).removeAttr("disabled");
            }

        });

        $("#edit_theme").on("click", function () {
            var self = this;
            $(this).attr("disabled", "disabled");
            var title = $("#theme_title_edit").val();
            if (title) {
                $.post("/city/edit", {title:title, theme_id : ++task_id},function (data) {
                    if (data.status === "ok") {
                        location.href = "/city";
                    } else {
                        alert("Ошибка при редактировании темы");
                    }
                }, "json").always(function () {
                    $(self).removeAttr("disabled");
                });
            } else {
                alert("Title не указан");
                $(self).removeAttr("disabled");
            }
        });

    });*/
</script>